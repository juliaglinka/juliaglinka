{
  "private": true,
  "devDependencies": {
    "parcel": "2.7.0",
    "purescript": "^0.15.4",
    "spago": "^0.20.9"
  },
  "scripts": {
    "build": "spago build",
    "serve": "spago build && ./serve.sh",
    "build:gh": "./build-gh.sh"
  }
}
